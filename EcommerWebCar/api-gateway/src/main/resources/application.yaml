server:
  port: 8888

app:
  api-prefix: /api/v1

spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: auth-service
              uri: http://localhost:8080
              predicates:
                - Path=${app.api-prefix}/auth/**
              filters:
                - StripPrefix=2

